# Zabbix Template Export

The custom Zabbix Docker template used by the Mini-SOAR lab will be exported manually from the Zabbix interface and stored in this directory later.

Suggested filename:

```text
docker-mini-soar.yaml
```

No generated or reconstructed template is included because the exported Zabbix configuration must remain the source of truth.
